{
  "name": "ai-translator",
  "description": "Chrome AI Translator extension.",
  "type": "module",
  "workspaces": [
    "chrome-extension",
    "packages/*",
    "pages/*"
  ],
  "scripts": {
    "prebuild": "WORKSPACE_ROOT=$(pwd) bun --filter '@libs/*' build",
    "build": "WORKSPACE_ROOT=$(pwd) bun --filter '@ai-translator/*' build",
    "postbuild": "bun scripts/zip.ts",
    "dev": "WORKSPACE_ROOT=$(pwd) bun --filter '!./' dev"
  },
  "author": "Plumbiu",
  "license": "MIT",
  "devDependencies": {
    "@types/adm-zip": "0.5.7",
    "@types/bun": "^1.2.21",
    "@types/chrome": "latest",
    "@types/dom-chromium-ai": "0.0.9",
    "adm-zip": "0.5.16"
  }
}